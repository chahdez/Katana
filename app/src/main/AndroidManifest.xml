<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.katana" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".actividades.MyActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--Login-->
        <activity android:name=".actividades.Login"></activity>
        <!--Usuarios-->
        <activity android:name=".actividades.usuarios.UsuariosActivity"></activity>
        <activity android:name=".actividades.usuarios.UsuarioDetalleActivity"></activity>
        <!--Clientes-->
        <activity android:name=".actividades.clientes.ClientesActivity"></activity>
        <activity android:name=".actividades.clientes.ClienteDetalleActivity"></activity>
    </application>

</manifest>
